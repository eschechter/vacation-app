<%= form_for(@vacation) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :start_date %>
  <%= f.date_field :start_date %>
  <%= hidden_field_tag "vacation[user_id]", @user_id %>
  <%= f.submit "Make the vacay!" %>
<% end %>
