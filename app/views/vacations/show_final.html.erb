<h1>FINALIZED</h1>
<h2><%= "#{@vacation.name} starting on #{@vacation.start_date.strftime('%m/%d/%Y')} and ending on #{@vacation.end_date.strftime('%m/%d/%Y')}"%></h2>
<%= render "show-helper" %>
<%= render "layouts/nav-bar" %>